# architecture: <project>

Current structure and data flow, as built. Not a plan and not a history: what is true of the code today. Why a mechanism was chosen belongs in `dev/decisions.md`.

## shape

The top-level components and what each owns.

## flow

The stages in order, what enters and leaves each, and the invariant each establishes.

## interfaces

The contracts between components that a change may not break silently.
